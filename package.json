{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend do projecto master class typescript.",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:server": "ts-node-dev --respawn --transpile-only ./src/index.ts"
  },
  "author": "Edilson Alexandre Cuamba",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.14",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.3"
  }
}
